local_costmap:
  global_frame: odom
  robot_base_frame: base_footprint # default base_footprint

  update_frequency: 10.0 # default 10.0
  publish_frequency: 10.0
  transform_tolerance: 10  #default 0.5

  static_map: false  
  rolling_window: true
  width: 2 # default 3
  height: 2 # default 3
  resolution: 0.05 # default 0.05

  # plugins:
  #   # - {name: static_layer,    type: "costmap_2d::StaticLayer"}
  #   - {name: obstacle_layer,  type: "costmap_2d::ObstacleLayer"}
  #   - {name: inflation_layer, type: "costmap_2d::InflationLayer"}
  